import React from "react";
import { Text, View, Image, StyleSheet, Button, TouchableOpacity, Alert, Pressable, SafeAreaView } from 'react-native';

export default function Settingscreen() {
    return (
        <View> 
            <Text>This is Settings Screen </Text>
        </View>
    );
}